import 'package:flutter/material.dart';

class WebinarTab extends StatelessWidget {
  const WebinarTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Webinar Tab"),
      ),
    );
  }
}
